# .zshenv - loaded for all zsh sessions (interactive, non-interactive, login, non-login)
# This is necessary for SSH remote command execution to have correct PATH

# Homebrew
eval "$(/opt/homebrew/bin/brew shellenv)"
